/**
 * This is a placeholder service file.
 *
 * Add domain logic here that is shared by multiple commands or events.
 * Keeping logic in `src/services/` instead of `src/commands/` makes it
 * easier to test and reuse across the project.
 *
 * Example: `greeting.ts` is implemented in this same directory.
 */

export function exampleUtility(value: string): string {
  return value.trim().toLowerCase();
}
